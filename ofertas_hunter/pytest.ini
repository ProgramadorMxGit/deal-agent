[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -ra --strict-markers
markers =
    smoke: smoke/integration tests (may require network or snapshots)
    integration: integration tests (DB or external services)
    slow: tests that take >2 seconds
